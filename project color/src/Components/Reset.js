import React, { Component } from 'react';

class Reset extends Component {
  render(){
    return(
        <button type="button" className="btn btn-lg btn-primary">Reset</button>
    );
  }
}

export default Reset
